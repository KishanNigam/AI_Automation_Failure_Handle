# Coding Rules

Never bypass QueueManager.

Never access queue.json directly.

Never duplicate business logic.

Prefer extension over modification.

Every sprint must include testing.

Every sprint must preserve backward compatibility.

Never remove logging.

Use dataclasses.

Use type hints.

Use modular design.

Production-quality code only.